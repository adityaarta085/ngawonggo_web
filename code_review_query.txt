I have modified the PWA installation logic to allow the native Chrome "Mini-infobar" on mobile devices while keeping a custom installation UI on desktop.

Changes:
1. Updated `src/hooks/usePWA.js`:
   - Added a check for mobile devices using `navigator.userAgent`.
   - On mobile, I removed `e.preventDefault()` in the `beforeinstallprompt` event listener. This is the standard way to allow the native browser install promotion UI.
   - On desktop, `e.preventDefault()` is still called to support the custom UI.

2. Updated `src/components/InstallPWA.js`:
   - Added the same mobile device check.
   - Modified the component to return `null` early on mobile devices.
   - Removed the mobile-specific JSX from the component to ensure it doesn't conflict with or overlap the native UI.

Does this implementation correctly follow Google Chrome's PWA documentation for allowing the native Install Promotion UI on mobile?
